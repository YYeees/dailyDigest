# 排优先级标准

这份文档给每次跑digest排序的Claude Code参照用——**只讲"怎么判断内容质量高低"**，不讲机械的处理流程。判断本身要靠读，不是靠关键词匹配。

> 处理流程(要不要抓全文、要不要写摘要)的路由规则维护在`config.py`里，`rank_items.py pending`的输出会给每条内容直接带`deep_read_eligible`/`always_summarize`两个标记——不用在这份文档里重新判断走哪条路径，具体怎么执行见`.claude/skills/run-digest/SKILL.md`。

## 判断范围

对每条`pending`条目，同时给两个独立的track打分，**不合并成一个分数**：

| 字段 | 取值 |
|---|---|
| `ai_tier` / `ai_reason` | `high` / `medium` / `low` + 一句话理由(哪条标准命中了) |
| `anchor_tier` / `anchor_reason` | `high` / `medium` / `low` + 一句话理由 |
| `digest_summary` | 仅当`deep_read_eligible`且判到medium/high，或`always_summarize`为true时需要 |

同一条内容完全可能一边high一边low(比如Craig Mod的《Blood Robots》两边都high，大部分内容只沾一边或都不沾)。

## Track 1：AI实操/趋势

判断标准：这条内容对"用户成为AI高手"有多大帮助。

| Tier | 判断依据 |
|---|---|
| **high** | 同时满足"内容扎实"+"门槛不高"：①前沿模型/工具的新发布新趋势，且讲清楚是什么、为什么重要(不是纯新闻通稿)；②直接影响日常怎么用AI工具的实操信息(比如"Claude Code默认开启了什么新模式")；③面向普通使用者(非纯工程师)的AI应用指南/选型建议 |
| **medium** | ①沾AI边但偏宏观叙事/轻量，或创作者自己的推广/项目介绍；②需要一定技术背景才看得懂门道(模型内部机制、研究论文级别)——当前用户水平"一般"，按Mollick/Willison这个难度爬坡，超出的默认medium或low，不给high；③编程agent实战案例这类偏娱乐/演示性质的内容 |
| **low** | ①跟AI完全无关；②**同一来源里作者自己维护的小工具版本更新日志**——哪怕作者本身是AI领域的人，也要按内容本身判断，不能因为"这个人是AI高手"就把他所有产出都算AI实操内容 |

**两条硬性提醒**：
- **同一信源必须逐条判断，不能按信源整体定同一个tier**——这是实测(Simon Willison feed)验证过的真实坑：他的feed里混了真正的AI趋势内容和自己写的小工具(datasette/llm-anthropic等)版本更新日志，后者没有普适学习价值。
- **重复报道**：同一件事被同一源连续追踪好几篇时，挑一条信息量最全的给正常tier，其余标注低一档，reason写"与XX重复，择一看即可"，避免主视图被同一件事刷屏。

## Track 2：thought-lab锚点

判断依据是`/Users/taoye/claude/thought-lab/now/关注锚点清单.md`(**每次判断前重新读一遍**，这份清单用户自己维护，可能会变)。三根主干：

| 根 | 主题 |
|---|---|
| 根一 · 深度自我认识 | 透过荣格/哲学/历史/文学等镜头追问人如何自处 |
| 根二 · AI作为认知方式 | 不止技术，是认知方式；和LLM共建/harness/认知系统 |
| 根三 · 清净的场域 | 有选择地应对纷扰，不是逃避 |

| Tier | 判断依据 |
|---|---|
| **high** | 明确踩中某条正向母题(见清单原文，比如"一手不二手""自然生长质朴中正""护城河在harness不在模型")，且是作者本人的**一手反思/经历**，不是转述 |
| **medium** | 主题沾某根主干的边(不是直接讨论母题本身)，或者是二手/周边内容 |
| **low** | 跟三根主干和周边领域都没有实质连接 |

**重要例外——不要让锚点清单变成关键词过滤器**：清单原文有条元规则"清单只管连已知，清单外让你意外的东西不受此约束，别让清单把品味冻住"。内容明显高质量、有洞察但主题在清单外时，不要因为"关键词对不上"就自动给low——用"这条内容本身够不够有分量"判断，可以标`medium`并在reason写清楚"清单外但值得注意"。

## 内容排除(`excluded_reason`)

`ai_tier`/`anchor_tier`控制的是"值不值得放进精选"，但X来源不管tier全展示(见`config.ALWAYS_DISPLAY_TYPES`)——所以对X内容想真正"不展示"，光判低分没用，要显式填`excluded_reason`(一句话理由)，`digest_summary`等字段照常写，只是导出时会被过滤掉。**这是内容主题层面的排除，靠读懂内容判断，不是关键词硬匹配**——同一个意思换种说法表达，也要能认出来。

已定的具体规则(2026-08-16)：

**Ray Dalio的X内容**，命中下面任一条就排除：
- 反复重申"开放心态(open-mindedness)"或"极度真实/极度透明(radical truth / radical transparency)"这几条Principles里的核心观点的语录体内容——他会用不同措辞反复讲同一件事，认出主题在重复，不是只匹配字面词。他Principles里其他主题(比如决策方法、痛苦+反思=进步)不在这条排除范围内，只排除这两个主题。
- 个人生活类动态，跟他的Principles/投资工作没有实质关系(比如夜店庆生这类)。

以后再新增别人的排除规则，也按这个格式补在下面，不要另起一份文档。

---

加新信源前的一手账号核实清单，见`sources.py`文件头注释(不是每次排序都要做的事，只在新增信源时用)。
