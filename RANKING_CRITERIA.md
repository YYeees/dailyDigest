# 排优先级标准

这份文档给每次跑digest排序的Claude Code参照用,不是代码规则。判断本身要靠读,不是靠关键词匹配。

## 输入与流程:两段式判断(2026-08-14定案)

从`digest.db`里,读还没排过序的条目(`ranked_at IS NULL`):标题、来源、来源类型(`source_type`)、发布日期、摘要(`summary`字段)。

**第一段·初筛(所有pending条目都要做,不抓取任何东西)**:只用标题+`summary`字段,按下面Track 1/Track 2的标准给出初步`ai_tier`/`anchor_tier`。

**第二段·精判(只针对满足条件的条目)**:
- **`source_type == blog`且初筛任一track达到`medium`或`high`**:WebFetch链接抓一次全文,基于全文重新判断该track的tier(可能上调也可能下调),并生成`digest_summary`。全文只在这一步临时读取,生成完立刻丢弃,**不落盘**。
- **`source_type`是`podcast`/`youtube`**:不追加抓取——这类来源的`summary`字段本身就是作者/平台写的节目简介,已经是对内容的提炼,WebFetch节目页/视频页拿到的通常还是同一份简介,信息量不会更多。直接用初筛的tier定档,`digest_summary`基于已有`summary`做一次轻提炼(不是照抄,也不需要额外抓取)。
- **`source_type == blog`但初筛两个track都是`low`**:维持`low`,不生成`digest_summary`(low不进展示层,没必要花这个成本)。

## 输出

对每条内容,填这几个字段:
- `ai_tier` / `ai_reason`
- `anchor_tier` / `anchor_reason`
- `digest_summary`(仅当至少一个track是`medium`或`high`时需要)
- 各自取值 `high` / `medium` / `low`,reason一句话说清楚判断依据(哪条标准命中了)

两个track完全独立判断,不合并、不用一个综合分数替代。同一条内容完全可能一边high一边low(比如Craig Mod的《Blood Robots》两边都high,大部分内容只沾一边或都不沾)。

## Track 1: AI实操/趋势

判断标准:这条内容对"用户成为AI高手"有多大帮助。

**给high的情况**(需要同时满足"内容扎实"+"门槛不高"):
- 前沿模型/工具的新发布、新趋势,且讲清楚了是什么、为什么重要(不是纯新闻通稿)
- 直接影响用户日常怎么用AI工具的实操信息(比如"Claude Code默认开启了什么新模式")
- 面向普通使用者(不是纯工程师)的AI应用指南/选型建议

**给medium的情况**:
- 沾AI边,但偏宏观叙事、轻量、或者是内容创作者自己的推广/项目介绍(达里欧的Digital Twin、纳瓦尔的Live in the Future 都是这类——聊AI但不是"怎么用AI"或者"AI界发生了什么大事")
- 需要一定技术背景才看得懂门道的内容(比如涉及模型内部机制、研究论文级别的分析)——当前用户水平"一般"、按Mollick/Willison这个难度爬坡,超出这个难度的默认给medium或low,不给high
- 编程agent实战案例这类"有意思但偏娱乐/演示性质"的内容

**给low的情况**:
- 跟AI完全无关的内容
- **同一个来源里,作者自己维护的小工具的版本更新日志**(比如Simon Willison自己写的Python库的release note)——这类内容对"学AI实操"没有普适价值,哪怕作者本身是AI领域的人,也要按内容本身判断,不能因为"这个人是AI高手"就把他的所有产出都算AI实操内容。**同一个信源里的内容必须逐条判断,不能按信源整体给同一个tier**——这是实测(Simon Willison feed)验证过的真实坑。

**重复报道处理**:如果同一件事被同一个源连续发好几篇(比如"AI agent在安全测试中意外攻击了其他公司"这种系列追踪报道),不用每条都拉到同一个tier——**挑一条信息量最全的标注正常tier,其余标注低一档,reason写"与XX重复,择一看即可"**,避免主视图被同一件事刷屏。

## Track 2: thought-lab锚点

判断依据是`/Users/taoye/claude/thought-lab/now/关注锚点清单.md`(每次判断前建议重新读一遍,这份清单由用户自己维护,可能会变)。三根主干:
- 根一·深度自我认识(透过荣格/哲学/历史/文学等镜头追问人如何自处)
- 根二·AI作为认知方式(不止技术,是认知方式;和LLM共建/harness/认知系统)
- 根三·清净的场域(有选择地应对纷扰,不是逃避)

**给high的情况**:内容明确踩中某条正向母题(见锚点清单原文,比如"一手不二手""自然生长质朴中正""护城河在harness不在模型"),且是作者本人的一手反思/经历,不是转述。

**给medium的情况**:主题沾某根主干的边(比如历史类内容对应根一,但不是直接讨论"人如何自处"这个母题本身),或者是二手/周边内容。

**给low的情况**:跟三根主干和周边领域都没有实质连接。

**重要例外——不要让锚点清单变成关键词过滤器**:清单原文有一条元规则,"清单只管连已知,清单外让你意外的东西不受此约束,别让清单把品味冻住"。如果一条内容明显是高质量、有洞察、但主题在清单外,不要因为"关键词对不上"就自动给low——用"这条内容本身够不够有分量"去判断,可以标`anchor_tier: medium`并在reason里写清楚"清单外但值得注意"，而不是强行往三根主干上硬靠，也不是直接忽略。

## 一手/二手判断(适用于两个track)

信源本身要核实是不是本人一手账号,不是看起来像就行——**踩过的真坑**：`navalsarchive.substack.com`域名看着像本人,实际是第三方粉丝解读账号("Naval's writings, explained"),内容是对纳瓦尔的分析转述,不是他本人写的。已经改用他真正的官方播客`nav.al/feed`。以后加新源时,第一次加入前务必核实运营者身份(查"About"页面、查有没有本人在其他已验证渠道自认这个账号),不能只测RSS技术上能不能用。

## 全文与摘要生成(只在这一步才碰全文)

具体规则见上面"输入与流程:两段式判断"一节。核心原则不变:全文只在blog类精判这一步临时读取,生成完`digest_summary`立刻丢弃,**不落盘**;最终存进`digest_summary`字段的只有AI生成的短摘要(内容概要+核心观点,不是全文摘录/照抄)。

**X来源的内容例外**:不用走"抓全文生成摘要"这一步——推文本身已经很短,`digest_summary`直接用原文文本就行,不需要额外总结。（这部分逻辑等X抓取代码写出来后才会真正用上。）
